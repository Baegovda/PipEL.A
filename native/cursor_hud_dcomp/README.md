# Cursor HUD (shim)

**Canonical source:** `cpp/src/native/hud_dcomp/`

Build:

```bat
native\cursor_hud_dcomp\build_dcomp.bat
```

or

```bat
cpp\src\native\hud_dcomp\build.bat
```

Output: `cursor_hud_dcomp.dll` (copied next to `Pipela.exe` by CMake POST_BUILD).

Do not add duplicate `.cpp` here — see `docs/STRUCTURE.md`.
